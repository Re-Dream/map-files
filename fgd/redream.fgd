@include "base.fgd"

@PointClass base(PlayerClass, Angles) studio("models/editor/playerstart.mdl") sphere(Size) = redream_spawn :
	"This entity acts as a spawn point, it will find the nearest available space to spawn the player."
[
	Size(Integer) : "Size" : 128 : "The area which players will spawn in units."
]
